{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ospp-standard.org/schemas/v1/mqtt/data-transfer-request.schema.json",
  "title": "DataTransferRequest",
  "description": "Request to transfer vendor-specific data between station and server (bidirectional).",
  "type": "object",
  "required": ["vendorId", "dataId"],
  "additionalProperties": false,
  "properties": {
    "vendorId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "description": "Identifies the vendor or extension author."
    },
    "dataId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "description": "Identifies the data type or command within the vendor namespace."
    },
    "data": {
      "type": "object",
      "description": "Vendor-defined JSON payload. Structure is not validated by the protocol."
    }
  }
}
