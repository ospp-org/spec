{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ospp-standard.org/schemas/v1/mqtt/authorize-offline-pass-response.schema.json",
  "title": "AuthorizeOfflinePassResponse",
  "description": "Server response to an AuthorizeOfflinePassRequest, granting or denying the offline pass authorization.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "status"
  ],
  "properties": {
    "status": {
      "type": "string",
      "enum": [
        "Accepted",
        "Rejected"
      ],
      "description": "Result of the offline pass authorization."
    },
    "sessionId": {
      "$ref": "../common/session-id.schema.json",
      "description": "Assigned session identifier. Present when status is Accepted."
    },
    "durationSeconds": {
      "type": "integer",
      "minimum": 1,
      "description": "Authorized service duration in seconds. Present when status is Accepted."
    },
    "creditsAuthorized": {
      "$ref": "../common/credit-amount.schema.json",
      "description": "Number of credits authorized for this session. Present when status is Accepted."
    },
    "reason": {
      "type": "string",
      "maxLength": 256,
      "description": "The registry NAME of the error, in UPPER_SNAKE_CASE (e.g. \"OFFLINE_PASS_EXPIRED\"), not prose. This response carries no errorCode field and is not gaining one: it travels server->station, where a station validating against an older vendored copy would refuse an unknown property outright. The name is what makes the codes this action's Error Responses table lists machine-readable, and it needs no schema shape change to do it. Present when status is Rejected."
    }
  },
  "allOf": [
    {
      "if": {
        "properties": {
          "status": {
            "const": "Accepted"
          }
        },
        "required": [
          "status"
        ]
      },
      "then": {
        "required": [
          "sessionId",
          "durationSeconds",
          "creditsAuthorized"
        ]
      }
    },
    {
      "if": {
        "properties": {
          "status": {
            "const": "Rejected"
          }
        },
        "required": [
          "status"
        ]
      },
      "then": {
        "required": [
          "reason"
        ]
      }
    }
  ]
}
