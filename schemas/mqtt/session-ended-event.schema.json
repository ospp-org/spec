{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ospp-standard.org/schemas/v1/mqtt/session-ended-event.schema.json",
  "title": "SessionEnded",
  "description": "Station notification reporting the autonomous end of a session due to timer expiry or hardware fault.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "sessionId",
    "bayId",
    "reason",
    "actualDurationSeconds",
    "creditsCharged"
  ],
  "properties": {
    "sessionId": {
      "$ref": "../common/session-id.schema.json"
    },
    "bayId": {
      "$ref": "../common/bay-id.schema.json"
    },
    "reason": {
      "type": "string",
      "enum": ["TimerExpired", "Fault", "Local", "LocalOutOfCredit", "Deauthorized"],
      "description": "Why the session ended autonomously. TimerExpired: durationSeconds elapsed. Fault: hardware fault detected. Local: user manually stopped at the station (e.g., physical Stop button). LocalOutOfCredit: offline credit pool exhausted mid-session. Deauthorized: offline pass revoked mid-session via RevocationEpoch bump. See spec/03-messages.md §5.4 for full descriptions and spec/04-flows.md §6 for refund policy per reason."
    },
    "actualDurationSeconds": {
      "type": "integer",
      "minimum": 0
    },
    "creditsCharged": {
      "type": "integer",
      "minimum": 0
    },
    "meterValues": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "liquidMl": {
          "type": "integer",
          "minimum": 0
        },
        "consumableMl": {
          "type": "integer",
          "minimum": 0
        },
        "energyWh": {
          "type": "integer",
          "minimum": 0
        }
      }
    },
    "seqNo": {
      "type": "integer",
      "minimum": 0,
      "description": "Optional per-session monotonic counter (matches the running seqNo of the last MeterValues emitted for the session). See spec/02-transport.md §3.2."
    },
    "finalSeqNo": {
      "type": "integer",
      "minimum": 0,
      "description": "Optional canonical session-final marker — the highest seqNo emitted for this session. Servers MUST discard any MeterValues with seqNo > finalSeqNo received after this SessionEnded event for the same sessionId. See spec/02-transport.md §3.2."
    }
  }
}
